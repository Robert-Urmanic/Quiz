<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Local development defaults. In other environments these are
             overridden by DB_URL / DB_USER / DB_PASSWORD (see HibernateUtil),
             so deployment credentials never live in this file.
             The driver is resolved from the URL, so switching database engine
             only means pointing DB_URL somewhere else. -->
        <property name="connection.url">jdbc:sqlserver://URMANICR-DH4241:1433;databaseName=Quiz;encrypt=false</property>
        <property name="connection.username">testDB</property>
        <property name="connection.password">testDB</property>

        <property name="show_sql">true</property>
        <property name="format_sql">true</property>

        <property name="hbm2ddl.auto">update</property>

        <mapping class="org.example.entity.Chapter"/>
        <mapping class="org.example.entity.Subchapter"/>
        <mapping class="org.example.entity.Question"/>
    </session-factory>
</hibernate-configuration>