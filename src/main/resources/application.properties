# Hosting platforms assign the port at runtime; 8080 stays the local default.
server.port=${PORT:8080}

# Listen on all interfaces so the container is reachable from outside it.
server.address=0.0.0.0
